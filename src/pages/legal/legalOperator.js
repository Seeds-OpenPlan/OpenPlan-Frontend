/*
  🔴 운영자 확정이 필요한 값 — 코드에서 알아낼 수 없는 것만 여기 모았다.

  약관·방침 본문의 나머지 문장은 전부 저장소의 코드·스키마에서 확인한 사실이지만,
  아래 다섯 값은 코드 어디에도 없다(운영 주체·책임자·연락처·인프라 사업자·시행일).
  지어내면 그 순간 법적 고지가 허위 사실이 되므로 한 파일에 모아 두고, 확정 전에는
  화면에 확정 필요 표시가 그대로 보이게 둔다 — 안 채운 채로 배포되면 눈에 띄어야 한다.

  구글 OAuth 심사는 이 페이지의 URL 을 직접 열어 확인한다. 확정 값을 채운 뒤
  배포해야 심사에 제출할 수 있다.
*/
const TBD = '[운영자 확정 필요]'

export const OPERATOR = {
  /* 서비스 명칭. 대외 공식 표기가 'OpenPlan' 이 맞는지 확인 필요. */
  serviceName: 'OpenPlan',

  /* 개인정보 보호책임자 성명·직책. 팀 대표 1인을 지정해야 한다. */
  privacyOfficer: TBD,

  /* 문의 이메일. 인증 메일 발신 주소와 같은 것을 쓸지 정해야 한다. */
  contactEmail: TBD,

  /* 클라우드 사업자 표기. 리전까지 밝힐지 함께 정할 것(국외 이전 고지와 맞물린다). */
  cloudProvider: TBD,

  /* 인증 메일 발송 경로 사업자. */
  mailProvider: TBD,
}

/* 시행일. 배포일에 맞춰 확정한다. */
export const EFFECTIVE_DATE = TBD
